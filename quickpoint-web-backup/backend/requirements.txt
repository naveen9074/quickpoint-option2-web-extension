# ── Core FastAPI / Server ──────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9         # required for UploadFile
pydantic==2.7.1
pydantic-settings==2.2.1

# ── Database ───────────────────────────────────────────────────────────────────
sqlalchemy==2.0.30
# For PostgreSQL swap the line below with: psycopg2-binary==2.9.9
# SQLite is built-in (no extra package)

# ── AI / ML ───────────────────────────────────────────────────────────────────
openai-whisper==20231117
torch==2.3.0                    # CPU build; for GPU: torch==2.3.0+cu121
transformers==4.41.2
accelerate==0.30.0              # speeds up HuggingFace inference
soundfile==0.12.1               # audio duration detection

# ── Audio Processing (Enhancement Module) ─────────────────────────────────────
librosa==0.10.2                 # time-stretching, audio loading
noisereduce==3.0.2              # spectral-gating noise reduction
scipy==1.13.0                   # required by noisereduce
numpy>=1.24.0                   # numeric arrays

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.0.1
